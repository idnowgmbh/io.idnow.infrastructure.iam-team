# ============================================================================
# Root Pipeline - Orchestrates Child Pipelines for Multi-Service Repository
# ============================================================================

stages:
  - trigger

variables:
  # Global variables passed to child pipelines
  DOCKER_TAG: $CI_COMMIT_SHORT_SHA
  DOCKER_LATEST_TAG: latest
